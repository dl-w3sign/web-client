export enum RPC_ERROR_MESSAGES {
  hashCollision = 'execution reverted: TimeStamping: Hash collision.',
  alreadySigned = 'execution reverted: TimeStamping: User has signed already.',
  isNotAdmitted = 'execution reverted: TimeStamping: User is not admitted.',
}
