export enum BUTTON_STATES {
  waiting = 'waiting',
  notAllowed = 'not-allowed',
  noneEvents = 'none-events',
}

export enum BUTTON_PRESETS {
  outlineBrittle = 'outline-brittle',
  primary = 'primary',

  outlinePrimary = 'outline-primary',
  genius = 'genius',
  outlineAccent = 'outline-accent',
}
